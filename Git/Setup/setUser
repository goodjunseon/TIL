git config --global user.name "example_name"
git config --global user.email "userEmail@example.com"

📌 이메일 주소는 github에 등록한 이메일 주소로 설정해야한다. git commit log에 기록되는 정보이므로 정확하게 입력 해야한다.

설정 확인 : git config --global --list
